info:
  name: Sign Up
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "email": "mirzanajafov@gmail.com",
        "password": "Mirzemirze"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
