info:
  name: Sign In
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/auth/signin"
  body:
    type: json
    data: |-
      {
        "email": "mirzanajafov@gmail.com",
        "password": "Mirzemirze"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
